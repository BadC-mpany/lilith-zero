# TypeScript minimal example policy — static allow/deny, no taint tracking.

id: "ts-minimal-demo"
customer_id: "example-corp"
name: "TypeScript Minimal Demo"
version: 1

static_rules:
  search_web: "ALLOW"
  get_time: "ALLOW"
  query_database: "DENY"

taint_rules: []
resource_rules: []
protect_lethal_trifecta: false
