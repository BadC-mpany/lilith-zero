name: Build & Artifacts

on:
  workflow_call:
    inputs:
      retention-days:
        required: false
        type: number
        default: 5

permissions:
  contents: read

jobs:
  build:
    name: Build (${{ matrix.target }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            target: x86_64-unknown-linux-gnu
            artifact_name: lilith-zero-linux-x86_64
            binary_name: lilith-zero
          - os: windows-latest
            target: x86_64-pc-windows-msvc
            artifact_name: lilith-zero-windows-x86_64.exe
            binary_name: lilith-zero.exe
          - os: macos-latest
            target: x86_64-apple-darwin
            artifact_name: lilith-zero-macos-x86_64
            binary_name: lilith-zero
          - os: macos-latest
            target: aarch64-apple-darwin
            artifact_name: lilith-zero-macos-aarch64
            binary_name: lilith-zero
          - os: ubuntu-24.04-arm
            target: aarch64-unknown-linux-gnu
            artifact_name: lilith-zero-linux-aarch64
            binary_name: lilith-zero

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4
        with:
          persist-credentials: false

      - name: Install Rust Toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Rust Cache
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: lilith-zero -> target

      - name: Build Release Binary
        working-directory: lilith-zero
        run: cargo build --release --target ${{ matrix.target }} --locked --no-default-features

      - name: Prepare Artifact
        shell: bash
        run: |
          mkdir -p dist
          cp "lilith-zero/target/${{ matrix.target }}/release/${{ matrix.binary_name }}" "dist/${{ matrix.artifact_name }}"

      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact_name }}
          path: dist/${{ matrix.artifact_name }}
          retention-days: ${{ inputs.retention-days }}
          if-no-files-found: error
