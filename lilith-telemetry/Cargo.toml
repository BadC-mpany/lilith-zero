[package]
name = "lilith-telemetry"
version = "0.1.0"
edition = "2024"
license = "Apache-2.0"

[dependencies]
hex = "0.4"
chrono = "0.4"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[features]
disable_telemetry = []
