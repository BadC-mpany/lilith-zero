id: "test-policy"
customer_id: "test-customer"
name: "Test Policy for Integration"
version: 1
created_at: "2024-01-27T00:00:00Z"

static_rules:
  read_db: "ALLOW"
  # send_slack is implicitly denied because it is not listed.

taint_rules: []
